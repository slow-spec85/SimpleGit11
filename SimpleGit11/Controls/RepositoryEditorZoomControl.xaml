<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SimpleGit11.Controls.RepositoryEditorZoomControl"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Button
        x:Name="EditorZoomButton"
        x:Uid="EditorZoomButton"
        Height="32"
        MinWidth="64"
        HorizontalContentAlignment="Center"
        Padding="8,0">
        <TextBlock>
            <Run Text="{x:Bind EditorSurface.ZoomFactor, Mode=OneWay}" />
            <Run Text="%" />
        </TextBlock>
        <Button.Flyout>
            <Flyout Placement="BottomEdgeAlignedRight">
                <StackPanel
                    Width="260"
                    Spacing="8">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Uid="EditorZoomFlyoutHeaderTextBlock"
                            VerticalAlignment="Center"
                            Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        <TextBlock
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="{x:Bind EditorSurface.ZoomFactor, Mode=OneWay}" />
                            <Run Text="%" />
                        </TextBlock>
                    </Grid>
                    <Slider
                        x:Name="EditorZoomSlider"
                        x:Uid="EditorZoomSlider"
                        IsThumbToolTipEnabled="True"
                        LargeChange="25"
                        SmallChange="10"
                        StepFrequency="1"
                        Value="{x:Bind EditorSurface.ZoomFactor, Mode=TwoWay}" />
                    <Button
                        x:Uid="ResetEditorZoomButton"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Click="ResetEditorZoomButton_Click" />
                </StackPanel>
            </Flyout>
        </Button.Flyout>
    </Button>
</UserControl>
